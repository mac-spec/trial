import { BrainCircuit, CheckCircle2, Database, GitBranch, ShieldCheck } from 'lucide-react';
import { isolationForestModel } from '@/ml/isolationForestModel';

type ModelIcon = typeof BrainCircuit;
export function ModelIntelligencePanel(){
 const cards:Array<{label:string;value:string|number;Icon:ModelIcon}>=[{label:'Trees',value:isolationForestModel.n_estimators,Icon:GitBranch},{label:'Training rows',value:isolationForestModel.training_samples,Icon:Database},{label:'Features',value:isolationForestModel.feature_names.length,Icon:BrainCircuit},{label:'Decision',value:'ML + rules',Icon:ShieldCheck}];
 return <section className="rounded-xl border border-slate-800 bg-slate-900 p-4 sm:p-5"><div className="flex flex-col sm:flex-row sm:items-start justify-between gap-3"><div className="flex items-start gap-3"><div className="w-10 h-10 rounded-xl bg-amber-500/10 border border-amber-500/20 flex items-center justify-center"><BrainCircuit className="w-5 h-5 text-amber-400"/></div><div><p className="text-[10px] uppercase tracking-widest text-amber-500">Machine intelligence</p><h3 className="text-sm sm:text-base font-semibold text-white mt-1">Isolation Forest anomaly engine</h3><p className="text-[10px] sm:text-xs text-slate-500 mt-1 max-w-2xl">Unsupervised outlier detection is combined with explainable compliance signals. The model flags unusual work profiles; it does not declare fraud.</p></div></div><span className="inline-flex items-center gap-1 text-[9px] font-bold uppercase tracking-wider text-emerald-400 bg-emerald-500/10 border border-emerald-500/20 rounded-full px-2.5 py-1"><CheckCircle2 className="w-3 h-3"/> Inference ready</span></div><div className="grid grid-cols-2 lg:grid-cols-4 gap-2 mt-4">{cards.map(({label,value,Icon})=><div key={label} className="rounded-lg bg-slate-950 border border-slate-800 p-3"><Icon className="w-4 h-4 text-slate-500"/><p className="text-[9px] uppercase tracking-wider text-slate-600 mt-2">{label}</p><p className="text-xs font-semibold text-slate-200 mt-1">{value}</p></div>)}</div><div className="mt-4 flex flex-wrap gap-1.5">{isolationForestModel.feature_names.map(f=><span key={f} className="text-[9px] text-slate-400 border border-slate-800 rounded-md px-2 py-1 bg-slate-950">{f.replace(/_/g,' ')}</span>)}</div><p className="text-[9px] text-slate-600 mt-3">Model provenance: trained offline with a reproducible synthetic administrative-pattern dataset for the SIH prototype. Production deployment should retrain on authorised historical MPLADS records and validate thresholds before operational use.</p></section>}
